{
  final int buttonColor=Utils.resolveColor(getContext(),R.attr.button_color);
  if (newPrimaryColor == 0)   newPrimaryColor=buttonColor;
  int[][] states=new int[][]{new int[]{-android.R.attr.state_enabled},new int[]{}};
  int[] colors=new int[]{Utils.adjustAlpha(buttonColor,0.6f),newPrimaryColor};
  return new ColorStateList(states,colors);
}
