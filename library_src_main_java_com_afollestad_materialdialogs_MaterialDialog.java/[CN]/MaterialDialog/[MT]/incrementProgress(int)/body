{
  if (mBuilder.progress <= -2)   throw new IllegalStateException("Cannot use incrementProgress() on this dialog.");
  mHandler.post(new Runnable(){
    @Override public void run(){
      setProgress(getCurrentProgress() + by);
    }
  }
);
}
