{
  if (maxLength < 1)   throw new IllegalArgumentException("Max length for input dialogs cannot be less than 1.");
  this.inputMaxLength=maxLength;
  this.inputMaxLengthErrorColor=errorColor;
  return this;
}
